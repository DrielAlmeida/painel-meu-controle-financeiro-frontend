import { Card } from "@/components/ui/card";import { formatCurrency } from "@/lib/utils";
export default function Page(){return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Planejamento financeiro</h1><p className="text-sm text-slate-500">Projeções são estimativas com base no ritmo atual.</p></div><div className="grid gap-4 md:grid-cols-3">{[["Renda mensal",5150],["Gastos fixos",1320],["Valor disponível",3966.93]].map(([t,v])=><Card key={String(t)}><p className="text-sm text-slate-500">{t}</p><p className="mt-2 text-2xl font-bold">{formatCurrency(Number(v))}</p></Card>)}</div><Card><h2 className="text-lg font-bold">Previsão do mês</h2><p className="mt-3 text-xl">Se você mantiver esse ritmo, terminará o mês com <strong className="text-emerald-600">{formatCurrency(2460)}</strong>.</p><p className="mt-2 text-sm text-slate-500">Estimativa baseada nos gastos registrados até hoje.</p></Card></div>}

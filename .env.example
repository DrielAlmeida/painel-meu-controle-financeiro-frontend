# URL pública da API FastAPI. Em produção, troque pelo domínio HTTPS da API.
VITE_API_URL=http://localhost:8000/api/v1

# Número oficial que recebe os gastos (DDI + DDD + número, somente dígitos).
VITE_WHATSAPP_ENTRY_NUMBER=5527998967373

import { Card } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { useAuth } from "@/components/auth/auth-provider";
export default function Page(){const{usuario}=useAuth();return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Meu perfil</h1><p className="text-sm text-slate-500">Dados atuais da sua conta.</p></div><Card className="max-w-2xl"><div className="grid gap-4"><label className="grid gap-1 text-sm">Nome<Input value={usuario?.nome??""} readOnly/></label><label className="grid gap-1 text-sm">Telefone<Input value={usuario?.telefone??""} readOnly/></label><label className="grid gap-1 text-sm">E-mail<Input value={usuario?.email??""} placeholder="Não cadastrado" readOnly/></label><p className="text-xs text-slate-500">A edição de dados pessoais ainda não possui endpoint na API. Quando ele for criado, esta tela poderá salvar alterações.</p></div></Card></div>}
